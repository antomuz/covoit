<form method="post" action="{{ path('app_commentaire_delete', {'id': commentaire.id}) }}" onsubmit="return confirm('{{'convoit.supprimer_confirmation'|trans}}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commentaire.id) }}">
    <button class="btn">{{'convoit.supprimer'|trans}}</button>
</form>
