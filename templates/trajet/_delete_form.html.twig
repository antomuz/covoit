<form method="post" action="{{ path('app_trajet_delete', {'id': trajet.id}) }}" onsubmit="return confirm('{{'convoit.supprimer_confirmation'|trans}}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trajet.id) }}">
    <button class="btn">{{'convoit.supprimer'|trans}}</button>
</form>
